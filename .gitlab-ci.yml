image: golang:1.24

stages:
  - test

test:
  stage: test
  script:
    - go vet ./...
    # Note: Tests using the 'vision' build tag are skipped in CI (no OpenCV).
    # Default 'go test ./...' only runs stub/non-OpenCV tests.
    - go test ./... -race -count=1 -timeout 120s
    - go build ./...
